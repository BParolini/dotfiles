# A link must be created using the following command:
# ln -s $HOME/.dotfiles/apps/gradle.properties $HOME/.gradle/gradle.properties

org.gradle.caching=true
org.gradle.daemon=true
org.gradle.parallel=true
